template(name="dash")
  .dashboard-container
    .diagnosis
      +reactiveTable collection=diseases settings=tableSettings
      .symptoms
        each symptoms
          .label(style="background-color:#{color}")
            if selected
              | &#10004;&nbsp;#{this}
            else
              | #{this}
    .pane-container
      .pane
        +graph
      .pane
        +table
      .pane
        +dendrogram
      .pane
        +testPane
