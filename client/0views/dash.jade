template(name="dash")
  .dashboard-container
    .diagnosis
      +reactiveTable collection=diseases settings=tableSettings
      .features
        .feature-section.keywords
          h4 Keywords
          each diseases
            if selected
              each keywords
                .label(style="background-color:{{color name}}")
                  p keyword: #{name}
                  p categories:
                  each categories
                    .category #{this}
        .feature-section.dates
          h4 Dates
          each features
            if isDate
              a(href="http://en.wikinews.org/wiki/Wikinews:{{dateLink dateInformation}}") #{text}
        .feature-section.case-counts
          h4 Case Counts
          each features
            if eq type "caseCount"
              .label #{text}
        .feature-section.death-counts
          h4 Death Counts
          each features
            if eq type "deathCount"
              .label #{text}
        .feature-section.locations
          h4 Locations
          each features
            if eq type "cluster"
              p Cluster Center:
                a(href="https://www.google.com/maps/preview/@#{centroid.latitude},#{centroid.longitude},8z") #{centroid.latitude}, #{centroid.longitude}
              each locations
                .location #{name}, {{[country code]}}
    .pane-container
      .pane
        +geomap
      .pane
        +correlation
      .pane
        +dendrogram
      .pane
        +timeline
      .pane
        +graph
      .pane
        +text
