template(name="gridResultList")
  div(class="search-results")
    h4 Search Results:
    each results
      div(class="search-result")
        div
          a(href="{{gridUrl}}/#{id}", target="_blank") #{name}
    else
      p No Results Found
