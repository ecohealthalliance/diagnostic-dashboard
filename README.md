Diagnostic Dashboard Set-up
===========================

Create a config file that sets the variables from config.sample then run this command:

    ssh -i key.pem ubuntu@instance GIT_USER=user GIT_PASSWORD=password 'bash -s' < install.sh
